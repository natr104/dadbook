<h2>Activities</h2>
<ul>
    <% @activities.each do |a| %>
        <li><%= link_to a.name, a %><%= button_to "X", { action: "destroy", id: a.id }, method: "delete", data: { confirm: "Delete activity?" } %></li>
    <% end %>
</ul>
<%= link_to "Add activity", action: 'new' %>