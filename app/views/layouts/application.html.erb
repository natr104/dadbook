<!DOCTYPE html>
<html lang="en-AU">
  <head>
    <title>Dadbook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <h1><%= link_to "Dadbook", root_path %></h1>
      <% if current_user %>
        <%= link_to "#{current_user.welcome}", user_path(current_user.id) %>
        <%= link_to "Logout", logout_path, method:"post" %>
      <% else %>
        <%= link_to "Login", login_path %> <%= link_to("Login with Github", '/auth/github', method: :post) %> <%= link_to "Sign up", new_user_path %>
      <% end %>
      <br>
      <% if notice %>
        <%= notice %>
      <% end %>

      <% if alert %>
        <%= alert %>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
